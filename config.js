import { STRIPE_PUBLISHABLE_KEY, API_URL } from '@env';

const CONFIG = {
  STRIPE_PUBLISHABLE_KEY,
  API_URL,
  // Add any other configuration variables here
};

export default CONFIG;

// Named exports for easy import
export { STRIPE_PUBLISHABLE_KEY, API_URL };
